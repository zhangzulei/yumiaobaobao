package com.yumiaobaobao.app.user.token.service;

import com.yumiaobaobao.app.user.entity.user_token;

import java.util.List;

public interface user_tokenService {
    int addToken( user_token token);

    int updataToken( user_token token);

    List<user_token> findByUserId( String userid);
}
